# random_number_generator_tk
Random number generator with Tkinter. Supports integers and decimals, custom decimal precision, input normalization, copy-to-clipboard, multilingual UI (EN/ES), and a persistent session history with reloadable configurations. Includes error handling and a clean, responsive interface.
